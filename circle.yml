dependencies:
  pre:
    - npm install -g jspm
    - jspm config registries.github.auth $JSPM_GITHUB_AUTH_TOKEN

test:
  override:
    - sbt test
  post:
    - sbt riffRaffArtifact

general:
  - "target/riffraff"
